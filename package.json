{
  "name": "clocisco",
  "version": "1.0.0",
  "description": "A tiny browser extension which closes Cisco Secure Client browser window notifications after 10 seconds.",
  "main": "index.js",
  "scripts": {
    "start": "npm run dev",
    "dev": "tsc --watch",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chrome-types": "^0.1.246",
    "typescript": "^5.2.2"
  }
}
